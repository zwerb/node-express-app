<div class="breadcrumb-wrapper p-4 box-shadow">
	<div class="nav-wrapper">
		<div class="col s12">
			{{#if parent}}
				<span class="breadcrumb">{{parent}}</span>
			{{/if}}
			<span class="breadcrumb">{{title}}</span>
		</div>
	</div>
</div>
